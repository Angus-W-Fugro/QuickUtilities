<Window x:Class="SimplePassthrough.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimplePassthrough"
        mc:Ignorable="d"
        Title="Simple Passthrough"
        d:DataContext="{d:DesignInstance Type={x:Type local:MainModel}}"
        Height="450" Width="800">
    
    <Grid Margin="8,8,0,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Margin="0,0,8,8" Text="Incoming Port" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
            </Grid>
            <ComboBox Grid.Row="1" Margin="0,0,8,8" />
        </Grid>
    </Grid>
</Window>
